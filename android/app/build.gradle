plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.undhu.meter"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    lintOptions {
        checkReleaseBuilds false
        }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "com.undhu.meter"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        minSdk = 24
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }
//    repositories {
//        flatDir {
//            dirs 'libs'  // This tells Gradle to look for .aar files in the libs directory
//        }
//    }
}

flutter {
    source = "../.."
}

dependencies {
    //this is from chat gpt--------
    // Add the Ola Maps SDK dependency
//    implementation(name: 'OlaMapSdk-1.5.0', ext: 'aar')
//    implementation 'com.android.support:support-v4:28.0.0'  // Ensure required dependencies are added


    //this is from Ola official website-------
    //OlaMap SDK
//    implementation(files("libs/OlaMapSdk-1.5.0.aar"))

//Maplibre
//    implementation ("org.maplibre.gl:android-sdk:10.0.2")
//    implementation ("org.maplibre.gl:android-plugin-annotation-v9:1.0.0")
//    implementation ("org.maplibre.gl:android-plugin-markerview-v9:1.0.0")
}

